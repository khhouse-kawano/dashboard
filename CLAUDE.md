# ワークフローと改修ルール
- 複雑なタスクや既存コードの改修を行う際は、必ず事前に変更計画を提示し、ユーザーの承認（許可）を得てから実装に着手すること。
- 既存の動作するコードを、明示的な指示なく勝手にリファクタリングしないこと。
- テストコードやドキュメントを、確認なしに削除・変更・コメントアウトしないこと。

# セキュリティと禁止事項
- `.env` やクレデンシャル（パスワードやAPIキー等）が含まれるファイルは、いかなる理由があっても読み書きしないこと。
- システム全体に影響を与える破壊的なコマンド（`rm -rf`, DBの削除など）は実行しないこと。
- 実行すべきか迷うコマンドや処理については、必ずユーザーに事前確認すること。

# コミュニケーション
- 応答は日本語で行うこと（コードや変数名は英語で可）。
- 返答は簡潔に行い、長々とした自明な説明は省略すること。

# コードスタイル（※プロジェクトに合わせて適宜変更してください）
- （例）TypeScriptの型は厳密に定義し、`any`の乱用は避けること。
- （例）エラーは握りつぶさず、適切なログ出力を実装すること。