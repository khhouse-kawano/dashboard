# 指示（2026-09-21）　販促媒体別広告費に「その他（未分類）」行を足す

⚠️ 依頼（`ReadMeClaude.md`）:
> `CustomerKaeru.tsx` 改修
> * 総額が ShopKaeru より少ない => 該当項目に含まれないものがあると想定されるので
>   **その他**として表示できないか

---

## ⚠️ 調査の結果、原因は2つに分かれていた

⚠️⚠️ **「総額が少ない」の主因は媒体ではなく店舗だった。**

| 原因 | 金額 | どこで直したか |
|---|---|---|
| ⚠️ **`budget.shop` に係でない行が混ざっていた** | ⚠️ **¥19,684,754** | ⚠️ **v2.2.138 のSQLで解決済み** |
| ⚠️ **`medium_kaeru` に無い媒体** | ⚠️ **¥5,540,846** | ⚠️ **今回の「その他（未分類）」行** |

⚠️ 前者の経緯は `docs/oldDocument/task-2026-09-18-06-budget-shop-cleanup.md` を参照。
⚠️ ⚠️ **総額そのものは v2.2.138 で一致済み**（ShopKaeru = CustomerKaeru = ¥298,891,204）。

⚠️ 今回直すのは ⚠️ **「総反響」行と、各媒体行の合計が合わない**という別の食い違いである。

---

## 変更したファイル

| ディレクトリ | ファイル | 内容 |
|---|---|---|
| `frontend/src/components/customer/` | ⚠️ `CustomerKaeru.tsx` | ⚠️ **定数1・`useMemo` 1・判定2か所** |

⚠️ ⚠️ **バックエンドの改修は無い。** ⚠️ `medium_kaeru` にも行を足していない。

---

## ⚠️ なぜ消えていたか

⚠️ `aggregated` は ⚠️ **足し算**で集計している（⚠️ 引き算にしていない）。

```tsx
            const base = filteredCustomers.filter(c => {
                if (value.medium === '総反響') return true;
                const medium = normalizeMedium(c.medium);
                if (value.medium === HOMEPAGE_ROW) return groupedMediums.includes(medium);
                return medium === value.medium;
            });
```

⚠️⚠️ **どの行にも当てはまらない媒体は、黙って消えていた。**

⚠️ これは ⚠️ **意図してそうしてあった**（2026-09-16 のコメント）。

> ⚠️ 「総反響から show_graph=1 の分を引く」形にはしていない。
>   ⚠️ 媒体が空だったり medium_kaeru に無い値の反響が混ざると、
>     ⚠️ 引き算では**それらが黙って「ホームページ反響」に入る。**
>   ⚠️ 足し算なら、拾えていない反響は表に出ない＝気づける。

⚠️ ⚠️ **「気づける」はずが、実際には誰も気づいていなかった**ので、⚠️ **行にして見せることにした。**

---

## ⚠️ 実データ（ローカル・`section = 'spec'` / `response_medium = 0`）

| 媒体 | 金額 | `medium_kaeru` に | `MEDIUM_ALIAS` で寄る先 |
|---|---|---|---|
| インターネット検索 | ¥105,045,842 | 無い | ⚠️ **Web検索** |
| SNS広告 | ¥93,702,101 | 無い | ⚠️ **Instagram** |
| SUUMO | ¥69,596,750 | ⚠️ **ある** | — |
| athome | ¥14,025,407 | 無い | ⚠️ **アットホーム** |
| 紹介 | ¥5,468,215 | ⚠️ **ある** | — |
| HOME'S | ¥3,787,102 | ⚠️ **ある** | — |
| ⚠️ **Amazonギフトカード** | ⚠️ **¥3,600,828** | ⚠️ **無い** | ⚠️ **無し → その他（未分類）** |
| ⚠️ **タウンライフ** | ⚠️ **¥1,416,845** | ⚠️ **無い** | ⚠️ **無し → その他（未分類）** |
| チラシ | ¥801,819 | ⚠️ **ある** | — |
| カゴスマ | ¥605,022 | 無い | ⚠️ **カゴスマ・タテルヤ** |
| ⚠️ **イベント** | ⚠️ **¥336,798** | ⚠️ **無い** | ⚠️ **無し → その他（未分類）** |
| 公式LINE | ¥318,100 | ⚠️ **ある** | — |
| ⚠️ **販促物** | ⚠️ **¥106,226** | ⚠️ **無い** | ⚠️ **無し → その他（未分類）** |
| ⚠️ **LP制作** | ⚠️ **¥66,399** | ⚠️ **無い** | ⚠️ **無し → その他（未分類）** |
| ⚠️ **イエタッタ** | ⚠️ **¥13,750** | ⚠️ **無い** | ⚠️ **無し → その他（未分類）** |

⚠️⚠️ **「その他（未分類）」の合計は ¥5,540,846。**

---

## ⚠️ 追加した定数

```tsx
/**
 * ⚠️⚠️ **`medium_kaeru` に無い媒体を受け止める行の名前**（2026-09-18 の指示）。
 *
 * ⚠️ この行を足すまで、⚠️ **各媒体の行を足しても「総反響」に届かなかった。**
 *   ⚠️ `aggregated` は**足し算**で集計しており（引き算にしていない）、
 *     ⚠️ どの行にも当てはまらない反響・販促費は**黙って消えていた。**
 *   ⚠️ 実測（ローカル・建売の販促費）で ⚠️ **¥5,540,846** が消えていた
 *     （タウンライフ / Amazonギフトカード / イベント / 販促物 / LP制作 / イエタッタ）。
 *
 * ⚠️⚠️ **`medium_kaeru` の `その他` とは別物である。**
 *   ⚠️ あちらは `show_graph = 0` なので **`ホームページ反響` にまとめられる。**
 *   ⚠️ 同じ名前にすると二重に数えられるので、⚠️ **`（未分類）` を外さないこと。**
 *
 * ⚠️ この行が大きいときは ⚠️ **`medium_kaeru` に媒体を足すか、
 *   customerKaeruUtils.ts の `MEDIUM_ALIAS` に別名を足す**のが正しい対応である。
 */
const OTHER_ROW = 'その他（未分類）';
```

⚠️⚠️ **`medium_kaeru` に `その他` という媒体が実在する。**
⚠️ ⚠️ **同じ名前にすると、`ホームページ反響` と二重に数えられる。**

---

## ⚠️ 追加・修正した関数

### `rows`（表の行を決める）

```tsx
    const rows = useMemo<Medium[]>(() => {
        const base: Medium[] = [
            { medium: '総反響' },
            ...shownMediums.map(medium => ({ medium })),
        ];
        // ⚠️ まとめる媒体が1つも無ければ、空の行を作らない
        const withHomepage = groupedMediums.length > 0 ? [...base, { medium: HOMEPAGE_ROW }] : base;

        /**
         * ⚠️⚠️ **「その他（未分類）」は件数が0でも必ず出す**（2026-09-18 の指示）。
         *   ⚠️ 0 のときこそ「取りこぼしが無い」という情報になる。
         *   ⚠️ 条件付きにすると、⚠️ **行が消えたのか取りこぼしが無いのか分からない。**
         * ⚠️ 位置は**いちばん最後**。個別の媒体より先に出すと内訳に見えて誤読される。
         */
        return [...withHomepage, { medium: OTHER_ROW }];
    }, [shownMediums, groupedMediums]);
```

### ⚠️ `knownMediums`（新規）

```tsx
    /**
     * ⚠️ `medium_kaeru` に載っている媒体（表記を寄せたあとの名前）。
     * ⚠️⚠️ **「その他（未分類）」はこれに**含まれない**ものを拾う。**
     *   ⚠️ 顧客側と販促費側で**同じ配列**を使うこと。片方だけだと単価が合わない。
     */
    const knownMediums = useMemo(
        () => [...shownMediums, ...groupedMediums],
        [shownMediums, groupedMediums]
    );
```

### `aggregated` の顧客側

```tsx
                const medium = normalizeMedium(c.medium);
                if (value.medium === HOMEPAGE_ROW) return groupedMediums.includes(medium);
                // ⚠️ どの行にも当てはまらない反響。⚠️ **空の媒体もここに入る。**
                if (value.medium === OTHER_ROW) return !knownMediums.includes(medium);
                return medium === value.medium;
```

### `aggregated` の販促費側

```tsx
                    const medium = normalizeMedium(item.medium);
                    if (value.medium === HOMEPAGE_ROW) return groupedMediums.includes(medium);
                    // ⚠️ 顧客側と同じ判定。⚠️ **揃えないと単価が合わない。**
                    if (value.medium === OTHER_ROW) return !knownMediums.includes(medium);
                    return medium === value.medium;
```

⚠️ 依存配列に `knownMediums` を追加:

```tsx
    }, [rows, groupedMediums, knownMediums, filteredCustomers, filteredBudgets]);
```

---

## ⚠️ これで何が言えるようになるか

⚠️⚠️ **「総反響」行 ＝ それ以外の行の合計** になる。

⚠️ ⚠️ **合わないときは集計のバグである**と、初めて言い切れる。

⚠️ また、⚠️ **「その他（未分類）」が大きくなったら媒体マスタの手入れが要る**という
運用のサインにもなる。

---

## ⚠️ やっていないこと

| # | 内容 | 理由 |
|---|---|---|
| 1 | ⚠️ `medium_kaeru` に `タウンライフ` 等を足す | ⚠️ **マスタの追加は業務判断** |
| 2 | ⚠️ `MEDIUM_ALIAS` に別名を足す | ⚠️ 同上（⚠️ `イベント` や `販促物` は媒体ではない可能性がある） |
| 3 | ⚠️ `ShopKaeru.tsx` に同じ行を足す | ⚠️ **あちらの行は店舗**。媒体の取りこぼしという概念が無い |
| 4 | ⚠️ グラフから「その他（未分類）」を除く | ⚠️ `graphData` は `aggregated` をそのまま使う。⚠️ **X軸に出る** |

---

## 検証

| 確認 | 結果 |
|---|---|
| ⚠️ 「その他（未分類）」に入る販促費 | ⚠️ **¥5,540,846**（⚠️ SQL の一覧から手計算で確認） |
| ⚠️ `medium_kaeru` の `その他` と衝突しないか | ⚠️ **しない**（名前が違う） |
| `npm run build` | ⚠️ **成功** |
| ⚠️ `CustomerKaeru.tsx` の新規警告 | ⚠️ **0件** |

### ⚠️ 未実施

⚠️⚠️ **Docker Desktop が起動していないため、顧客側（反響数）の実測ができていない。**

| # | 確認 | 期待 |
|---|---|---|
| 1 | 建売 → 販促媒体別広告費 | ⚠️ **最終行に「その他（未分類）」がある** |
| 2 | ⚠️ 各行の総予算の合計 | ⚠️ **「総反響」行と一致する** |
| 3 | ⚠️ 各行の総反響数の合計 | ⚠️ **「総反響」行と一致する** |
| 4 | ⚠️ 「その他（未分類）」の総予算 | ⚠️ **¥5,540,846**（全期間） |
| 5 | グラフ | ⚠️ **X軸の最後に出る** |
