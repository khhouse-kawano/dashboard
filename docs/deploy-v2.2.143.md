# デプロイ手順 v2.2.143

⚠️ この版には ⚠️ **3件**入っている。

| # | 中身 | 記録 |
|---|---|---|
| 1 | 競合サマリーの営業課を `section_list` から取る | `task-2026-09-22-04` |
| 2 | ⚠️ **建売のホームページ反響を CustomerTrendKaeru と同じ判定にする** | `task-2026-09-22-05` |
| 3 | ⚠️ **地図の Express 化・ステータス絞り込みの削除** | `task-2026-09-22-06` |

⚠️⚠️ **v2.2.142（MCPの道具・レポート画面の502修正）を先に通しておくこと。**

⚠️ ⚠️ **DB のテーブル変更は無い。** ⚠️ 追加する SQL は `update_log` の1行だけ。

---

## ⚠️ 利用者から見て変わること

| 画面 | 変わること |
|---|---|
| 他社動向 → 競合サマリー | ⚠️ 営業課が **6 → 7**。⚠️ **大分営業課・佐賀・久留米営業課が選べる**（⚠️ **これまで絞り込めなかった**） |
| ⚠️ **顧客分析（建売）** | ⚠️⚠️ **ホームページ反響の件数が増える**（実測 5,798 → **6,270**）。⚠️ **広告費も変わる**（**203,216,989**） |
| ⚠️ **地図（3事業とも）** | ⚠️ **「ステータスを選択」が消える** |

⚠️⚠️ **顧客分析（建売）の数字が変わることは、事前に利用者へ伝えておくこと。**
⚠️ ⚠️ **反響推移（建売）の「ホームページ反響計」と一致するようになる**のが狙いである。

---

## ⚠️ 順序

```
1. ① SQL（update_log）
2. ② Express の再ビルド        ← ⚠️ 先
3. ① PHP（3ファイル）
4. ① フロント                   ← ⚠️ 最後
```

⚠️⚠️ **② → ① フロント の順を守ること。**
⚠️ ⚠️ **地図は ② に登録していないと「ループ検知」で 502 になる。**
⚠️ （① に `map.php` があるのでフォールバックはするが、⚠️ **無駄な往復とログ汚れが出る**。）

⚠️ 2 と 3 はどちらが先でもよい。

---

## 手順1　【① レンタルサーバーで実行】SQL（phpMyAdmin）

```sql
INSERT INTO update_log (version, date, note) VALUES
('2.2.143', '2026-09-22', '競合サマリーの営業課の一覧を、画面の直書きから section_list に切り替えた。');
```

⚠️ ファイル: `backend/scripts/sql/2026-09-22_update_log_2.2.143.sql`

⚠️ ⚠️ **テーブルの作成・変更は無い。**

---

## 手順2　【② VPS で実行】Express の再ビルド

```bash
cd ~/dashboard
git fetch --depth 1 origin production
git checkout FETCH_HEAD
```

⚠️⚠️ **`git pull` は使わないこと。** ⚠️ **この環境は detached HEAD が正しい状態**で、必ず分岐エラーになる。

⚠️ 入ったことを ⚠️ **ファイルの中身で**確かめる（⚠️ `--depth 1` なのでログは1件しか無い）。

```bash
grep -c "地図の初期データ" backend-express/src/gateway/registry.ts
ls backend-express/src/features/map/
grep -c hp_campaign backend-express/src/features/customer/queries.ts
```

⚠️ ⚠️ **`queries.ts` と `index.ts` の2つが見えること。**

```bash
dcp build express-api
dcp up -d --force-recreate express-api
```

⚠️ コンテナの中にも入ったか確かめる。

```bash
dcp logs express-api | grep "map::"
```

⚠️ ⚠️ **起動時の一覧に `map::order` / `map::spec` / `map::used` の3行が出れば反映完了。**

⚠️⚠️ **`grep "map::order" dist/gateway/registry.js` では見つからない。**
⚠️ ⚠️ **`map::order` という文字列はソースに無い。**
⚠️ **キーは `gatewayKey(request, roll, category)` が実行時に組み立てている。**

⚠️ ファイルで見るならこちら。

```bash
dcp exec express-api grep -c "地図の初期データ" dist/gateway/registry.js
```

⚠️ **3 になれば入っている。**

---

## 手順3　【① レンタルサーバーで実行】PHP のアップロード

| ファイル | 置き場所 |
|---|---|
| `backend/src/handlers/competitor.php` | `handlers/` |
| ⚠️ **`backend/src/handlers/customerAction/customer_spec.php`** | `handlers/customerAction/` |
| ⚠️ **`backend/src/core/express_proxy.php`** | `core/` |

⚠️ ⚠️ **3ファイル。** ⚠️ `map.php` と `mapAction/` は ⚠️ **変更していないので触らない。**

---

## 手順4　【あなたのPC（PowerShell）】フロント → ① へアップロード

⚠️ ⚠️ **ビルド済み**（2026-09-22 13:54・`main.66ba3173.js`）。作り直す場合は:

```powershell
cd C:\Users\shinji-kawano\react\dashboard\frontend
npm run build
```

| 何 | 備考 |
|---|---|
| `index.html` | ⚠️ **必ず差し替える** |
| `static/js/` | 新しい `main.*.js` |
| `static/css/` | 新しい `main.*.css` |

---

## ⚠️ 手順5　動作確認

### 5-1　競合サマリー

| # | 確認 | 期待 |
|---|---|---|
| 1 | 他社動向 → 競合サマリー | ⚠️ **営業課が7つ** |
| 2 | 並び | ⚠️ **鹿児島1 → 鹿児島2 → 鹿児島3 → 宮崎 → 大分 → 熊本 → 佐賀・久留米** |
| 3 | ⚠️⚠️ **大分営業課を選ぶ** | ⚠️⚠️ **件数が出る**（⚠️ 前は選択肢自体が無かった） |
| 4 | 「大分・佐賀営業課」 | ⚠️ **選択肢から消えている** |

### ⚠️ 5-2　顧客分析（建売）

| # | 確認 | 期待 |
|---|---|---|
| 1 | ⚠️ **ホームページ反響の反響数** | ⚠️ **6,270 前後**（⚠️ 旧 5,798） |
| 2 | ⚠️⚠️ **反響推移（建売）の「ホームページ反響計」と突き合わせ** | ⚠️⚠️ **一致すること** |
| 3 | ⚠️ ホームページ反響の広告費 | ⚠️ **約 2億320万** |
| 4 | ⚠️ 各行の合計 | ⚠️ **単独行＋ホームページ反響＋その他 ＝ 総反響** |
| 5 | 「その他（未分類）」 | ⚠️ **288 前後**（⚠️ **0 にはならない**） |

⚠️ ⚠️ **2 が今回の眼目。** ⚠️ **画面ごとに違う数字が出ていたのを揃えている。**

### ⚠️ 5-3　地図

| # | 確認 | 期待 |
|---|---|---|
| 1 | 注文 / 建売 / 中古 | ⚠️ **3つとも開く** |
| 2 | ⚠️⚠️ **「ステータスを選択」** | ⚠️⚠️ **消えている**（3事業とも） |
| 3 | 残りの絞り込み | ⚠️ 販促媒体・ブランド・店舗・年収・契約種別が効く |
| 4 | ピンの数・エリア別一覧 | ⚠️ **移植前と同じ** |
| 5 | ⚠️ **② のログ** | ⚠️⚠️ **「ループ検知」が出ない** |

```bash
dcp logs --tail=30 express-api
```

### 5-4　変わっていないことの確認

| # | 画面 | 期待 |
|---|---|---|
| 1 | 顧客分析（注文） | ⚠️ **変わらない**（⚠️ 触っていない） |
| 2 | 反響推移（建売） | ⚠️ **変わらない**（⚠️ **こちらが正で、合わせにいった側**） |
| 3 | バージョン表示 | ⚠️ **2.2.143** |

---

## ⚠️ 戻し方

| 何 | どう戻すか |
|---|---|
| フロント | 1つ前の `main.*.js` と `index.html` |
| ① PHP | 1つ前の3ファイル |
| ② Express | 1つ前のコミットを checkout して `dcp build` |
| ⚠️ **地図だけ止めたい** | ⚠️ ① の `express_proxy.php` から `'map::*'` の3行を消す（⚠️ **① の PHP で動く**） |
| `update_log` の行 | ⚠️ 残しておいてよい |

⚠️ ⚠️ **地図は ① に PHP が実在するので、② を戻さなくても転送リストを外せば止まる。**

---

## ⚠️ この版で入っていないもの

| 何 | なぜ |
|---|---|
| ⚠️⚠️ **他の10ファイルの `axios` 直叩き** | ⚠️ 今回の指示は地図のみ。⚠️ **`BudgetAccounting` / `BudgetKaeru` / `Calendar` / `CallStatusList` などが本番URLを直に叩いている** |
| ⚠️ 他の画面の営業課の直書き | ⚠️ 指示は競合サマリーのみ |
| ⚠️ `consulting` 権限の伏字 | ⚠️ 次の版 |

---

## ⚠️ 長期の宿題

| # | 内容 |
|---|---|
| 1 | ⚠️⚠️ **地図の顧客一覧の `WHERE` が実質効いていない**（`or` でつながっている）。⚠️ 注文で24,515件を丸ごと転送している |
| 2 | ⚠️ ① の `map.php` は ⚠️ **移植後も残している**。⚠️ フォールバック用だが、二重管理ではある |
| 3 | ⚠️ `deploy-v2.2.141.md` の長期の宿題19項目も引き続き有効 |
