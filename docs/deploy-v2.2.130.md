# v2.2.130 デプロイ手順

## ⚠️⚠️ このリリースは今までより戻しにくい

**顧客一覧（`database`）を ② へ移した。** これは Dashboard で最も使われる画面である。

⚠️ **応答が大きい。** ローカル実測で

| category | 応答サイズ |
|---|---|
| `order`（注文） | **21,497,740 バイト（約 21.5MB）** |
| `spec`（建売） | 8,739,903 バイト（約 8.7MB） |

⚠️⚠️ **`forwardToExpress` の `CURLOPT_TIMEOUT` は 120 秒**である。
2026-09-11 に**別の request が 17MB・120 秒で切れている**。今回はそれより大きい。

⚠️ 切れてもフォールバックで ① が応答するため**画面は動く**。だが 120 秒待ってから
① が処理を始めるので、**利用者からは「顧客一覧が異常に遅い」**という見え方になる。
⚠️ **手順5-1 で必ず体感速度を確かめること。**

**戻し方は下の「ロールバック」にまとめてある。① の1ファイルから2行消すだけで戻る。**

---

## ⚠️ まず 126 の SQL がデプロイ済みか確認する

### 【① レンタルサーバー】phpMyAdmin で確認

```sql
SHOW COLUMNS FROM `medium_kaeru` WHERE `Field` = 'show_graph';
```

| 結果 | 意味 |
|---|---|
| 1行返る | ✅ 実行済み。そのまま進めてよい |
| ⚠️ 0行 | **手順0を実行する**（`backend/scripts/sql/2026-09-11_medium_kaeru_show_graph.sql`） |

⚠️ `show_graph` が無いと、今回の**広告費シミュレーターの建売で販促媒体別が
「ホームページ反響計」1行だけ**になる。エラーは出ない。
⚠️ ローカルDBでは**この列がまだ無かった**（2026-09-14 に確認）。本番も未実行の可能性がある。

⚠️ 建売の販促媒体別動向（`CustomerTrendKaeru.tsx`）も同じ列に依存している。

---

## このリリースの内容

`origin/production` に無いコミットは8本（v2.2.128 / v2.2.129 の分を含む）。

| 変更 | 場所 |
|---|---|
| ⚠️ 紹介キャンペーンでスタッフが選べない不具合の修正（年度の取り違え） | ① フロント |
| ⚠️ 架電記録が**他の顧客に保存される**不具合の修正 | ① フロント |
| 紹介キャンペーン反響一覧を1つの表にまとめ、顧客URLのコピーボタンを追加 | ① フロント |
| 建売・中古の顧客一覧で `?id=` を開けるように | ① フロント |
| 広告費シミュレーターの作り直し（KPI別単価・媒体別・契約目標・試算の2軸） | ① フロント ＋ ② ＋ ① PHP |
| 顧客一覧（`database` の order / spec）を Express へ移植 | ② ＋ ① PHP |
| 顧客一覧に**担当営業のセレクト**を追加（同姓の担当者対策） | ① フロント |
| `version.ts` を `2.2.130` に更新 | ① フロント |

### 今回**やらないこと**

- ⚠️ **SQL の変更は無い。** 必要なのは 126 の `show_graph` だけ（上の確認参照）。
- ⚠️ `.env.prod` の追記は無い。npm 依存の追加も無い（`--renew-anon-volumes` 不要）。
- ⚠️ 第3のサーバー（`kh-house.jp`）への作業は無い。

---

## サーバーの区別（毎回確認する）

| 呼び方 | 実体 | 役割 |
|---|---|---|
| **① レンタルサーバー** | Xserver 共用 / `khg-marketing.info` | 本番DB・PHP・React。管理は**サーバーパネル**／phpMyAdmin |
| **② VPS** | `162.43.5.127` / `api.khg-marketing.info` | Docker（express-api / caddy / ssh-tunnel）。**SSH** |

---

## ⚠️⚠️ 実行順序は変えてはいけない

```
0. 【① phpMyAdmin】 126 の SQL（未実行の場合のみ）      ← 最初
1. push → GitHub で PR マージ
2. 【② VPS】        Express を更新
3. 【①】            PHP を2ファイル
4. 【①】            フロントを build → アップロード     ← 最後
```

**なぜこの順序なのか**

- **② を ① の PHP より先にする理由**

  ⚠️ ① の許可リストに `database::order` を入れた状態で ② が古いと、
  ② が「ループ検知」で 502 を返す。① がフォールバックするので画面は動くが、
  ⚠️ **顧客一覧は全利用者が開く画面**なので、無駄な往復がそのまま負荷になる。

- **フロントを最後にする理由**

  ⚠️ 広告費シミュレーターのフロントは、応答に **`medium`（販促媒体マスタ）が
  含まれている前提**で書いてある。② と ① の PHP がどちらも古いと、
  **媒体別のブロックが1つも出ない。**

---

## 0. 【① レンタルサーバー】126 の SQL（未実行の場合のみ）

`backend/scripts/sql/2026-09-11_medium_kaeru_show_graph.sql` を phpMyAdmin の SQL タブへ。

⚠️⚠️ **先に中身の SELECT で実データの表記を目で見ること。**

```sql
SELECT `medium` FROM `medium_kaeru` ORDER BY `medium`;
```

⚠️ **`ALLGRIT` という値は存在しない。実データは `公式LINE`**（画面表示だけ
`mediumFormate()` が差し替えている）。`アットホーム` が `athome` のこともある。
⚠️ 表記が違うと **0行更新でもエラーが出ない。**

⚠️ 最後の確認クエリで **4行返ること**。

---

## 1. push → PR マージ

### 【あなたのPC（PowerShell）で実行】

⚠️ この手順書と `version.ts`（`2.2.130`）は**コミット済み**である。push だけでよい。

```powershell
cd C:\Users\shinji-kawano\react\dashboard
git push origin v2.2.130
```

### ⚠️⚠️ `git push origin HEAD:production` は使わない

PR のマージで `production` にマージコミットが付くため、ローカルのブランチは
`production` の子孫にならない。**構造的に必ず reject される。**

1. `git push origin v2.2.130`（ブランチまで）
2. GitHub で `v2.2.130` → `production` の PR を作る
3. PR をマージする

---

## 2. 【② VPS】Express の更新

```bash
ssh root@162.43.5.127
```

⚠️ 入り直したら毎回張り直す。

```bash
alias dcp='docker compose -f ~/dashboard/docker-compose.prod.yml --env-file ~/dashboard/.env.prod'
```

⚠️⚠️ **素の `docker compose` を使わないこと。** `.env.prod` を読まない。
以前これで caddy が空の `ACME_EMAIL` で作り直され、443 が落ちた。

```bash
cd ~/dashboard
git fetch --depth 1 origin production
git reset --hard FETCH_HEAD
git log --oneline -1
```

⚠️ 手順1の PR マージが**終わってから**実行する。

```bash
dcp build express-api
dcp up -d --force-recreate express-api
dcp ps
```

⚠️ npm 依存を追加していないので `--renew-anon-volumes` は不要。
⚠️ `caddy` / `express-api` / `ssh-tunnel` の3つすべてが `Up` であること。

### 2-1. 取り込み確認

⚠️⚠️ **認証なしの curl では「認証が必要です。」しか返らない**（`auth: 'staff'`）。
**ログで確かめるのが確実。**

```bash
dcp logs --tail 400 express-api | grep -E "database::|budget_simulator"
```

⚠️ 以下の3つが出ること。

```
🔒 database::order       — 顧客一覧の初期データ（order）：マスタ＋顧客＋ギフト進呈可否
🔒 database::spec        — 顧客一覧の初期データ（spec）：マスタ＋顧客＋ギフト進呈可否
🔒 budget_simulator::    — 広告費シミュレーターの初期データ（注文・建売をまとめて返す）
```

⚠️⚠️ **`database::used` と `database::common` は出ないのが正常。**
中古の顧客一覧（`DatabaseResale.tsx`）は移植していない。これまでどおり ① が応答する。

⚠️⚠️ **`database:trash:order` のような roll 付きも出ないのが正常。**
ゴミ箱・複製は書き込みを伴う別経路で、移植していない。

---

## 3. 【① レンタルサーバー】PHP のアップロード

サーバーパネル → ファイルマネージャ（または FTP）。
配置先は `dashboard/api/gateway/` 配下。

| ローカル | ① の配置先 |
|---|---|
| `backend/src/core/express_proxy.php` | `core/express_proxy.php` |
| `backend/src/handlers/budget_simulator.php` | `handlers/budget_simulator.php` |

⚠️ **この2つだけ。**

### ⚠️⚠️ `budget_simulator.php` は普段使われない。それでも上げること

② が処理するので、動くのは**転送が失敗したときだけ**である。
⚠️ 上げ忘れると、転送失敗時に**広告費シミュレーターが真っ白**になる
（返す形が旧版とまったく違うため）。
⚠️ 2026-09-11 に実際に転送失敗（`Resolving timed out`）が起きている。

### 確認【あなたのPC（PowerShell）で実行】

⚠️ **PowerShell の `-d "{\"...\"}"` は JSON が壊れる。** ファイル渡しにする。

```powershell
cd $env:TEMP
'{"request":"database","category":"order"}' | Out-File -Encoding ascii db.json
curl.exe -s -D - -o NUL -X POST https://khg-marketing.info/dashboard/api/gateway/ `
  -H "Content-Type: application/json" -H "Authorization: 4081Kokubu" -d "@db.json"
```

⚠️ 応答ヘッダに **`X-Handled-By: express`** が出ること。
本文が `認証が必要です。` になるのは正常（② まで届いた証拠）。

---

## 4. 【① レンタルサーバー】フロントの build

```powershell
cd C:\Users\shinji-kawano\react\dashboard\frontend
npm run build
```

⚠️ `Compiled with warnings.` は正常。`Failed to compile` なら止めて報告してほしい。

`frontend/build/` の中身を ① の公開ディレクトリへアップロードする。
⚠️ `index.html` の上書き漏れに注意（古い JS を読み続ける）。

---

## 5. 動作確認

⚠️ まず **Ctrl + F5**。バージョン表示が **2.2.130** であること。

### ⚠️⚠️ 5-1. 顧客一覧の速度（**最優先。ここが今回いちばん危ない**）

注文でログインし、**顧客一覧**を開く。

| 見え方 | 判断 |
|---|---|
| これまでと同じくらいで開く | ✅ 問題なし |
| ⚠️ **2分ほど固まってから開く** | **② への転送が 120 秒で切れている。ロールバックする** |
| 開かない・エラー | ロールバックする |

⚠️ 件数（画面上部の表示）がデプロイ前と**同じ**であること。

⚠️ 建売でも同じように顧客一覧を開いて確認する。

### 5-2. 担当営業のセレクト（⚠️ 今回の新規）

顧客一覧で**店舗を選ぶ**。

- ⚠️ 店舗セレクトの**右隣に青いセレクト**が出ること（店舗未選択では出ないのが正常）
- 候補が**役職順 → 社員番号順**で並ぶこと（店長が先頭）
- 選ぶと、その担当者の顧客だけに絞られること
- ⚠️ **店舗を変えると担当営業が外れる**こと（残ると0件になるため意図的にそうしている）
- ⚠️ 既存の担当者名のテキスト検索も**今までどおり**使えること

⚠️ 候補が**空**なら、その店舗に今年度（`period`）の営業職（`category = 1`）が
登録されていない。⚠️ **不具合ではなくデータ側**の可能性が高い。
実測（ローカル / 2026-09-14）では41店舗すべてで候補が出ている。

### 5-3. 広告費シミュレーター（⚠️ 大きく変わった）

ヘッダーのメニューから開く。

- 全画面で開くこと
- 表の**上に「試算の軸」**があり、**広告費を固定 / 単価を固定**が選べること
- ⚠️ 「入力欄＝昨年実績」「当期＝◯◯」の**説明が出ている**こと
- 各件数の下に**当期の実績が青字**で出ていること
- **販促媒体別**のブロックが並ぶこと
  - ⚠️ 注文で**ブロックが1つも出ない**なら、② か ① の PHP が古い
  - ⚠️ 建売で「ホームページ反響計」しか出ないなら、**手順0の SQL が効いていない**

**試算の確認**

1. 軸を **単価を固定**にする
2. 契約の件数を**1.5倍**に書き換える
3. ⚠️ **広告費も約1.5倍になる**こと（これが「契約数に必要な広告費」）
4. ⚠️ **4つの単価が変わらない**こと
5. 軸を **広告費を固定**に戻し、同じことをする
6. ⚠️ 今度は**広告費が動かず、契約単価だけ下がる**こと

### 5-4. 架電記録（⚠️ 不具合修正の確認）

⚠️ **顧客Aの詳細を開いて架電を1件登録 → 閉じる → 顧客Bを開く。**

- ⚠️ **Bの架電記録にAの記録が混ざっていないこと**
- 新規顧客を開いたときも、前の顧客の記録が残っていないこと

### 5-5. 紹介キャンペーン

- ⚠️ **KH久留米店を選んでスタッフが選べること**（v2.2.128 の修正）
- 未同期と同期済みが**1つの表**に出ること。同期済みの行に色が付くこと
- 顧客編集ページのURLをコピーするボタンが動くこと

### 5-6. 建売・中古の顧客一覧

- ⚠️ 中古の顧客一覧が**これまでどおり開くこと**（移植していないので変化が無いのが正常）
- ゴミ箱・複製が動くこと（⚠️ 移植していないので ① が応答する）

---

## ⚠️⚠️ ロールバック

### 顧客一覧が遅い・おかしい場合

① の `core/express_proxy.php` を開き、**この2行を消す**（またはコメントアウト）。

```php
'database::order',
'database::spec',
```

⚠️ これだけで顧客一覧は ① の PHP に戻る。**② もフロントも触らなくてよい。**
⚠️ フロントは ① と ② のどちらが応答しても同じ形を受け取る（下記の検証参照）。

### 広告費シミュレーターがおかしい場合

同じファイルから `'budget_simulator',` を消す。⚠️ ① の
`handlers/budget_simulator.php` が新しい形に書き換わっているので、**手順3で
上げていれば ① だけで動く。**

### 全部戻す場合

`frontend/build/` を前のバージョンに戻し、② を1つ前のコミットで
`dcp build` し直す。

---

## 参考: 今回できた検証

⚠️ **`database` は ① と ② の応答が MD5 まで一致することを確認済み**（ローカル）。

| category | バイト数 | MD5 |
|---|---|---|
| order | 21,497,740 | `df527dc48ac1bfe82e20a30e3615ab8f` |
| spec | 8,739,903 | `40cf7dda1fca744e37b6a540aeacaab9` |

⚠️ 両コンテナのハンドラを直接実行して比較したもの。**フォールバックしても
画面の見え方は変わらない。**

⚠️ `budget_simulator` は**まだバイト比較できていない**（① の形を作り替えたため、
比較対象が同一の実装になっており意味を持たない）。手順 5-3 の目視で確認する。

## ⚠️ 別途お願いしたいこと（データの修正）

`call_sheet` に店舗名の**表記ゆれ**がある（v2.2.127 から持ち越し）。

```sql
SELECT shop, COUNT(*) FROM call_sheet WHERE shop LIKE '%霧島%' GROUP BY shop;
```

⚠️ `PGH霧島店`（222件）と `PG HOUSE霧島店`（1件）の2通り。
今はコード側で両方拾っているが、本来はデータを直すのが筋である。

```sql
-- ⚠️ 実行は任意。直したら inside.ts と inside_list.php から
--    'PG HOUSE霧島店' の行を消してよい
UPDATE `call_sheet` SET `shop` = 'PGH霧島店' WHERE `shop` = 'PG HOUSE霧島店';
```

### ⚠️ 既に他の顧客に混ざってしまった架電記録

5-4 の修正は**今後の混入を止めるだけ**である。
⚠️ すでに保存されてしまった分は残っている。調査が必要なら別途お知らせしてほしい。
