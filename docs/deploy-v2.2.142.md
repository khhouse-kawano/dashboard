# デプロイ手順 v2.2.142

⚠️ 中身: ⚠️ **MCP サーバー（Claude Desktop）から競合分析とレポート保存ができるようにした。**

| # | 何が変わるか | どこ |
|---|---|---|
| 1 | ⚠️ **MCP サーバーの道具が4本 → 9本になる** | ⚠️⚠️ **利用者のPC** |
| 2 | 版の表示が `2.2.142` になる | ① フロント |
| 3 | 更新履歴に1行増える | ① DB |

⚠️⚠️ **② VPS の変更は無い。** ⚠️ **再ビルドは不要。**
⚠️ ⚠️ **① の PHP の変更も無い。**

⚠️⚠️ **前提: v2.2.141 が ①② に入っていること。**
⚠️ 入っていないと、⚠️ **MCP から `/analysis/competitor` を呼んでも 404 になる。**

---

## ⚠️ 順序

```
1. ① SQL（update_log）
2. ① フロント（ビルド成果物）
3. ⚠️ 利用者のPC（MCP サーバーの差し替え）
```

⚠️ 3 は ⚠️ **1・2 と独立している**ので、先でも後でもよい。
⚠️ ⚠️ **ただし v2.2.141 が ② に入っている必要がある。**

---

## 手順1　【① レンタルサーバーで実行】SQL

⚠️ phpMyAdmin で実行する。

⚠️ ファイル: `backend/scripts/sql/2026-09-22_update_log_2.2.142.sql`

```sql
INSERT INTO update_log (version, date, note) VALUES
('2.2.142', '2026-09-22', 'Claude Desktop（MCP）から競合分析とレポート保存ができるようにした。');
```

⚠️ `no` は AUTO_INCREMENT なので指定しないこと。

---

## 手順2　【あなたのPC（PowerShell）】フロントのビルド

```powershell
cd C:\Users\shinji-kawano\react\dashboard\frontend
npm run build
```

⚠️ 変更は `src/utils/version.ts` の1行だけだが、⚠️ **JSのハッシュが変わる。**

### 【① レンタルサーバーで実行】アップロード

| 何 | 備考 |
|---|---|
| `index.html` | ⚠️ **必ず差し替える**（JSのハッシュが変わっている） |
| `static/js/` | ⚠️ 新しい `main.*.js` |
| `static/css/` | ⚠️ 新しい `main.*.css` |

---

## ⚠️ 手順3　MCP サーバーを配り直す（⚠️ **利用者のPC**）

⚠️⚠️ **これがこの版の本体である。**

### 3-1　【あなたのPC（PowerShell）】ビルド

```powershell
cd C:\Users\shinji-kawano\react\dashboard\mcp-server
npm install
npm run build
```

⚠️ `dist/` が出来る。⚠️ **git には入っていない**（ビルド成果物）。

### 3-2　配る

⚠️ 配布のしかたは ⚠️ **`mcp-server/docs/manual-install-windows.md` のとおり**（変更なし）。

| 相手 | やること |
|---|---|
| ⚠️ **既に使っている人** | ⚠️ **`セットアップ.cmd` をもう一度実行** → ⚠️ **Claude Desktop を再起動** |
| 初めての人 | ⚠️ `mcp-server/docs/manual-install-windows.md` のとおり |

⚠️⚠️ **渡したフォルダを差し替えても意味がない。**
⚠️ `セットアップ.cmd` は ⚠️ **中身を `%LOCALAPPDATA%\khg-analysis-mcp` にコピーしてから**
⚠️ そこを `args` に書き込む。⚠️ ⚠️ **動いているのはコピー先である。**

⚠️ 手で差し替えるなら ⚠️ **コピー先の `dist` を上書きする**こと（`node_modules` はそのままでよい）。
⚠️ 実際の場所は ⚠️ **`args` を見れば確実**（`-InstallDir` を指定していれば別の場所になる）。

```powershell
(Get-Content "$env:APPDATA\Claude\claude_desktop_config.json" -Raw | ConvertFrom-Json).mcpServers.'khg-analysis'.args
```

⚠️⚠️ **`claude_desktop_config.json` は書き換え不要。**
⚠️ **接続先もAPIキーも変わっていない。**

⚠️⚠️ **Claude Desktop は × では終了しない。**
⚠️ ⚠️ **通知領域のアイコンを右クリック → 終了**してから起動し直すこと。
⚠️ **これをしないと設定が読み込まれない。**

⚠️ ⚠️ **APIキーは1人1本。** ⚠️ 使い回すと誰の操作か追えなくなる。

---

## ⚠️ 手順4　動作確認

### 4-1　Claude Desktop

| # | 聞くこと | 期待 |
|---|---|---|
| 1 | 「使えるツールを教えて」 | ⚠️ **9本出る**（⚠️ `get_competitor_deals` など5本が増えている） |
| 2 | ⚠️ **「どの競合に負けている？」** | ⚠️ 他社別の勝敗が返る |
| 3 | ⚠️ **「HTMLのレポートにして保存して」** | ⚠️ **保存できる**（⚠️ `no` が返る） |
| 4 | 「保存済みのレポートを一覧して」 | ⚠️ 一覧が返る |

⚠️ ⚠️ **2・3 は各自のアカウントで推論される。** ⚠️ **会社のAPIキーは使われない。**

### 4-2　ダッシュボード

| # | 確認 | 期待 |
|---|---|---|
| 1 | ⚠️ **他社動向 → Claudeによる競合分析** | ⚠️ **手順4-1で保存したレポートが一覧に出る** |
| 2 | 開く | ⚠️ 5段の経過ののち本文が出る |
| 3 | ⚠️ **登録者の札** | ⚠️⚠️ **`MCP` と出る**（画面から上げた分と区別が付く） |
| 4 | バージョン表示 | ⚠️ **2.2.142** |

### 4-3　個人情報

| # | 確認 | 期待 |
|---|---|---|
| 1 | ⚠️ **Claude Desktop の会話に出た商談データ** | ⚠️ **氏名・電話・メール・住所が無い** |
| 2 | ⚠️ メモの中 | ⚠️ **`****` になっている** |

⚠️⚠️ **1件でも氏名が出ていたら、その場で報告すること。**

---

## ⚠️ 戻し方

| 何 | どう戻すか |
|---|---|
| ⚠️ **MCP サーバー** | ⚠️ **1つ前の `dist` に戻す** → Claude Desktop を再起動 |
| フロント | 1つ前の `main.*.js` と `index.html` に戻す |
| `update_log` の行 | ⚠️ 残しておいてよい |

⚠️ ⚠️ **② には何も入れていないので、② を戻す必要は無い。**

---

## ⚠️ この版で入っていないもの

| 何 | なぜ |
|---|---|
| ⚠️ `consulting` 権限で全レスポンスを伏字にする | ⚠️ 利用者の判断で次の版へ |
| ⚠️ Express `/analysis` の既存エンドポイント（funnel / pivot）の建売対応 | ⚠️ 同上（⚠️ **競合分析だけは建売に対応済み**） |
| ⚠️ レポートの自動生成（定期実行） | ⚠️ **手で頼む運用から始める** |

---

## ⚠️ 長期の宿題

| # | 内容 |
|---|---|
| 1 | ⚠️⚠️ **`analysis_report` は1件60KB。** ⚠️ 溜まってきたら古い版の整理 |
| 2 | ⚠️ 12ヶ月分は ⚠️ **約74,000トークン**。⚠️ 各自のアカウントの上限に当たる可能性がある |
| 3 | ⚠️ 建売分譲事業は ⚠️ **競合の記録が実質ゼロ**（実測5件）。⚠️ 入力運用から |
| 4 | ⚠️ `deploy-v2.2.141.md` の長期の宿題19項目も引き続き有効 |
