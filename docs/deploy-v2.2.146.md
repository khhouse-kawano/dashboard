# デプロイ手順 v2.2.146

⚠️ 中身: ⚠️ **目標達成状況（rank）の見出しが縦書きになるのを直した。**

| # | 何が変わるか | どこ |
|---|---|---|
| 1 | ⚠️ **見出しが横書きになり、表が横スクロールする**（3事業とも） | ① フロント |
| 2 | 更新履歴に1行増える | ① DB |

⚠️⚠️ **② VPS の変更は無い。** ⚠️ **再ビルドは不要。**
⚠️ ⚠️ **① の PHP の変更も無い。** ⚠️ **MCP も変更なし。**

⚠️⚠️ **v2.2.145 のうち3コミットが未マージだったので、この版に含まれる。**

| 未マージだったもの | |
|---|---|
| ⚠️ **中古リノベの粗利額の丸め** | ⚠️ `584.3000000000001万円` の表示崩れ |
| デプロイ手順書の修正2件 | ドキュメントのみ |

---

## ⚠️ 順序

```
1. ① SQL（update_log）
2. ① フロント
```

⚠️ ⚠️ **どちらが先でもよい。** ⚠️ **依存関係は無い。**

---

## 手順1　【① レンタルサーバーで実行】SQL（phpMyAdmin）

```sql
INSERT INTO update_log (version, date, note) VALUES
('2.2.146', '2026-09-24', '目標達成状況の見出しが縦書きになる問題を修正（横スクロールに変更）。');
```

⚠️ ファイル: `backend/scripts/sql/2026-09-24_update_log_2.2.146.sql`

---

## 手順2　【あなたのPC（PowerShell）】フロント → ① へアップロード

⚠️ ⚠️ **ビルド済み**（2026-09-24）。

| ファイル | |
|---|---|
| ⚠️ **`build/static/js/main.23e7ebfe.js`** | ⚠️ **`2.2.146` を含む** |
| `build/static/css/main.7c10f266.css` | ⚠️ **変わっていない**（一緒に上げてよい） |
| ⚠️ **`build/index.html`** | ⚠️⚠️ **必ず差し替える** |

⚠️⚠️ **古いビルドを使わないこと。**
⚠️ `main.2c7d6cc9.js`（09-22 19:45）… ⚠️ **見出しが縦書きのまま**

⚠️⚠️ **コードを直したら必ずビルドし直し、ハッシュが変わったことを確かめること。**
⚠️ ⚠️ **2026-09-22 に、直したあとビルドせず古い版を上げて本番で崩れた。**

---

## ⚠️ 手順3　動作確認

| # | 画面 | 期待 |
|---|---|---|
| 1 | ⚠️ **目標達成状況（注文 / 建売 / 中古）** | ⚠️⚠️ **見出しが横書き**（⚠️ 縦に1文字ずつ並ばない） |
| 2 | ⚠️ 「粗利額/契約数(率)」 | ⚠️ **1行で出る** |
| 3 | ⚠️ 画面より表が広いとき | ⚠️⚠️ **表だけが横スクロールする**（⚠️ **ページ全体は動かない**） |
| 4 | 画面に収まるとき | ⚠️ **スクロールバーが出ない** |
| 5 | ⚠️ **中古リノベの粗利額** | ⚠️⚠️ **`584.3万円` のように丸まっている**（⚠️ v2.2.145 の積み残し） |
| 6 | 数字 | ⚠️ **改修前と同じ**（⚠️ 見た目だけの改修） |
| 7 | バージョン表示 | ⚠️ **2.2.146** |

---

## ⚠️ 戻し方

| 何 | どう戻すか |
|---|---|
| フロント | 1つ前の `main.*.js` と `index.html` |
| `update_log` の行 | ⚠️ 残しておいてよい |

⚠️ ⚠️ **フロントだけ戻せば元に戻る。** ⚠️ **サーバーには何も入れていない。**

---

## ⚠️ 申し送り

| # | 内容 |
|---|---|
| 1 | ⚠️⚠️ **表の幅をピクセルで決め打ちしないこと。** ⚠️ 列の数と文字数は事業ごとに違う |
| 2 | ⚠️ スクロールを持つのは ⚠️ **表だけ**。⚠️ **2か所が同時にスクロールすると見出しと中身がずれる** |
| 3 | ⚠️⚠️ **`rankingUi.tsx` の `<style>` にバッククォートを書かないこと。** ⚠️ **2026-09-22 と 09-24 に2度壊した** |
| 4 | ⚠️ `RankResale.tsx` は ⚠️ **表示方法を改修前のままにしてある**（2026-09-22 の指示）。⚠️ **CSSだけ借りている** |
