# デプロイ手順 v2.2.148

⚠️ 中身: ⚠️ **契約ランキングの達成率を独立した列にし、達成率でも並べ替えられるようにした。**

| # | 何が変わるか | どこ |
|---|---|---|
| 1 | ⚠️ **達成率が独立した列になる**（店舗別） | ⚠️ ① フロント |
| 2 | ⚠️ **達成率で並べ替えられる** | ⚠️ ① フロント |
| 3 | ⚠️⚠️ **実績日起算の数字が画面（反響推移）と一致する** | ⚠️ **② VPS** |
| 4 | ⚠️⚠️ **歩留まりの分母が画面と同じになる** | ⚠️ **② VPS** |
| 5 | ⚠️ **販促媒体の項目名が画面と同じになる**（注文も） | ⚠️ **② VPS** |
| 6 | 更新履歴に1行増える | ① DB |

⚠️⚠️ **② VPS の再ビルドが必要**（⚠️ 2026-09-25 に分析APIを直したため）。
⚠️ ⚠️ **① の PHP の変更も無い。** ⚠️ **MCP も変更なし。** ⚠️ **DB のテーブル定義も変えていない。**

---

## ⚠️⚠️ v2.2.147 が未マージなら、この版に含まれる

⚠️ ⚠️ **`v2.2.148` ブランチは `v2.2.147` から切ってある。**

| v2.2.147 の中身 | |
|---|---|
| ⚠️ **SUUMO掲載順位の保存を ② へ** | ⚠️ ① の `express_proxy.php` と ② の再ビルドが要る |
| ⚠️ **分析APIの実績日起算** | ⚠️ ② の再ビルドが要る |
| ⚠️ **建売の販促媒体の項目名** | ⚠️ ② の再ビルドが要る |

⚠️⚠️ **まだ 2.2.147 をデプロイしていないなら、[deploy-v2.2.147.md](deploy-v2.2.147.md) を先に通すこと。**
⚠️ ⚠️ **PR は1本で足りる**（v2.2.148 に 2.2.147 のコミットが入っているため）。

---

## ⚠️ 順序

```
1. ② VPS（Express の再ビルド）
2. ① SQL（update_log）
3. ① フロント
```

⚠️ ⚠️ **依存関係は無い。** ⚠️ どれが先でもよい。

---

## 手順0　【② VPS で実行】Express の再ビルド

⚠️ ⚠️ **`git pull` は使わないこと。** ⚠️ **必ず分岐エラーになる。**

```bash
cd ~/dashboard
git fetch --depth 1 origin production
git checkout FETCH_HEAD
```
```bash
dcp build express-api
```
```bash
dcp up -d express-api
```

---

## 手順1　【① レンタルサーバーで実行】SQL（phpMyAdmin）

```sql
INSERT INTO update_log (version, date, note) VALUES
('2.2.148', '2026-09-25', '契約ランキングの達成率を独立した列にし、達成率でも並べ替えられるようにした。分析APIの実績日起算と販促媒体を反響推移の画面に揃えた。');
```

⚠️ ファイル: `backend/scripts/sql/2026-09-24_update_log_2.2.148.sql`

---

## 手順2　【あなたのPC（PowerShell）】フロント → ① へアップロード

⚠️ ⚠️ **ビルド済み**（2026-09-24）。

| ファイル | |
|---|---|
| ⚠️ **`build/static/js/main.3bfc5388.js`** | ⚠️ **`2.2.148` を含む** |
| `build/static/css/main.7c10f266.css` | ⚠️ **変わっていない**（一緒に上げてよい） |
| ⚠️ **`build/index.html`** | ⚠️⚠️ **必ず差し替える** |

⚠️⚠️ **古いビルドを使わないこと。**
⚠️ `main.7f7ac5e8.js` … ⚠️ **v2.2.147**
⚠️ `main.899bbfff.js` … ⚠️ **v2.2.146**

⚠️⚠️ **コードを直したら必ずビルドし直し、ハッシュが変わったことを確かめること。**
⚠️ ⚠️ **2026-09-22 に、直したあとビルドせず古い版を上げて本番で崩れた。**

---

## ⚠️ 手順3　動作確認

⚠️ 画面: ⚠️ **会社実績 → 契約ランキング**

| # | 見るもの | 期待 |
|---|---|---|
| 1 | ⚠️ **店舗別タブ** | ⚠️⚠️ **「達成率」が独立した列で出る** |
| 2 | ⚠️ 達成率の見出し | ⚠️ **「総計÷予算」**と小さく出る |
| 3 | ⚠️ 開始月・終了月を選ぶ | ⚠️⚠️ **見出しが「期間計÷予算」に変わり、数字も変わる** |
| 4 | ⚠️⚠️ **達成率の見出しをクリック** | ⚠️⚠️ **達成率で並び替わる**（⚠️ もう一度押すと昇順） |
| 5 | ⚠️ 順位の数字 | ⚠️⚠️ **並びと一致する**（⚠️ 同着は同じ順位） |
| 6 | ⚠️ 達成率で並べたまま ⚠️ **個人別へ切り替え** | ⚠️⚠️ **壊れない**（⚠️ 総計順になる） |
| 7 | ⚠️ 個人別タブ | ⚠️ **予算・達成率の列が出ない** |
| 8 | ⚠️ 個人別の「所属」 | ⚠️ **契約を上げた店舗**（⚠️ 複数ならカンマ区切り） |
| 9 | ⚠️ 予算が0の店舗 | ⚠️⚠️ **`0%`**（⚠️ `Infinity%` や `NaN%` にならない） |
| 10 | バージョン表示 | ⚠️ **2.2.148** |

### ⚠️⚠️ 分析API（Claude Desktop から）

⚠️ 画面と突き合わせる: ⚠️ **反響推移（注文 / 建売）** と ⚠️ **同じ月・同じ数字になること。**

| # | 聞き方 | 期待 |
|---|---|---|
| 1 | 「2026年4月から6月の月別の反響と実来場と契約」 | ⚠️⚠️ **反響推移の画面と一致**（⚠️ 2026-05 の注文なら 1,068 / 375 / 86） |
| 2 | ⚠️ 同じ質問を建売で | ⚠️ 2026-05 が 296 / 接触224 / 来場126 / 申込48 / 契約42 |
| 3 | ⚠️ **歩留まりも出してもらう** | ⚠️⚠️ **meta の「比率の分母」が `nextAppointments ÷ visits`** になっている |
| 4 | ⚠️ **「媒体別の反響」** | ⚠️⚠️ **「ネット」が出ない。** ⚠️ 注文は `ホームページ反響計` が出る |
| 5 | ⚠️ 画面と数件ずれたら | ⚠️⚠️ **`report_flag` の差**（⚠️ JH八代店など。meta に明記済み） |

---

## ⚠️ 戻し方

| 何 | どう戻すか |
|---|---|
| フロント | 1つ前の `main.*.js` と `index.html` |
| ⚠️ **分析API** | ⚠️ ② を1つ前のコミットで再ビルドする |
| `update_log` の行 | ⚠️ 残しておいてよい |

⚠️ ⚠️ **① の PHP と DB には何も入れていない。**

---

## ⚠️ 申し送り

| # | 内容 |
|---|---|
| 1 | ⚠️⚠️ **並べ替えの対象を足すときは `valueOf()` だけを直すこと。** ⚠️ ソート・順位・足切りが同じものを見ている |
| 2 | ⚠️ 個人別の「所属」は ⚠️ **ステータスを見ていない**。⚠️ 件数0でも所属が出ることがある |
| 3 | ⚠️ 店舗別＋期間指定で ⚠️ **7列**になる。⚠️ **窮屈なら `size="lg"` を足す**（今は既定の md） |
| 4 | ⚠️ 達成率は ⚠️ **切り上げの整数**。⚠️ 99.1% は 100% と出る |
| 5 | ⚠️⚠️ **画面（customerTrend / customer）のKPI定義を変えたら `analysis/actual.ts` も直すこと** |
| 6 | ⚠️ 注文の実来場・次アポは ⚠️ **1人が複数月に立つ**（画面がそう）。⚠️ **月をまたいで合計してはならない** |
| 7 | ⚠️⚠️ **MCP の説明文「既定の reaction」が古いまま。** ⚠️ **次に MCP を配布する版で直すこと** |
