# 作業記録 2026-09-16

⚠️ このセッションで触ったもの一覧。⚠️ **未デプロイの項目が多い**ので末尾の一覧を必ず見ること。

---

## ブランチ

| ブランチ | 内容 | 状態 |
|---|---|---|
| `v2.2.131` | Google口コミ集計 | ⚠️ push済み・**未デプロイ** |
| `v2.2.132` | キャンペーンフォームの Express 化 | ⚠️ ローカル・**未push** |
| `v2.2.133` | MetaAds / 失注一覧 / キャンペーンUI | ⚠️ ローカル・**未push**（現在地） |

---

## 1. 経緯度取得の無駄打ち（請求 ¥150,000 の原因）

### 変更したファイル

| ディレクトリ | ファイル | 内容 |
|---|---|---|
| `projects/sync/src/services/` | `runGeocode.ts` | ⚠️ **全面書き直し** |
| `backend/src/handlers/` | `customer_address.php` | 更新行数を返す |
| `backend/src/handlers/` | `geoCode.php` | ⚠️ コメントのみ（**呼ばれなくなった**） |

### 追加した関数（`runGeocode.ts`）

- `fetchLatLng()` — ⚠️ 戻り値を `ok` / `nohit` / `error` / `fatal` の4種に分けた
- `save()` — ⚠️ **更新行数と `mode` を検証**して「黙って0行」を検知

### ⚠️ 原因

「物件データの処理」が**毎回 `property_db` の全1,031件**を Geocoding に投げ、⚠️ **1件も保存できていなかった**。

- 絞り込みが `latitude`/`longitude` を見ていたが、⚠️ **`property_db` にその列が無い**
- 送信キーが `property_number` だが、⚠️ **`property_db` のキーは `property_id`**
- 保存先の `geoCode.php` が ⚠️ **別テーブル `property_list_kaeru`** を更新

⚠️ さらにこの処理は**丸ごと不要**だった（同じ物件を `customer_address` 側が既に取得していた）。

### ⚠️ 残っている課題

- ⚠️ `master_data_resale` に **`id` が NULL の行**が1件。⚠️ 構造的に更新できない
- ⚠️ 1回500件の上限を掛けた。⚠️ **毎回上限に張り付くなら保存できていない疑い**

---

## 2. キャンペーンフォームの Express 化

### 追加したディレクトリ・ファイル

`backend-express/src/features/campaignForm/`

| ファイル | 内容 |
|---|---|
| `index.ts` | 管理画面用5件（list / detail / insert / update / master） |
| `entry.ts` | ⚠️ **公開受付**（entry / public）。認証なし |
| `mail.ts` | メール2通＋⚠️ **宛先の無害化** |
| `brands.ts` | ブランド対応表（⚠️ 4か所の写しを1つに集約） |
| `text.ts` | 入力の正規化・日付整形 |

`frontend/src/components/campaign/`

| ファイル | 内容 |
|---|---|
| `campaignApi.ts` | ⚠️ 新規。通信を1か所に |
| `formBuilderUtils.ts` | ⚠️ 新規。HTML生成・設定との対応表 |
| `FormBuilder.tsx` | ⚠️ 新規。LP用HTMLの生成 |
| `brands.ts` | ⚠️ 新規。ブランド表記・並び順 |
| `NewCampaign.tsx` | ⚠️ `components/` から**移動** |

`backend/forms/form-proxy.php` — ⚠️ 新規。8サイトに置くプロキシ

### 追加した関数

- `safeAddressList()`（`mail.ts` / `form-proxy.php`）— ⚠️ **メールヘッダの無害化**
- `usedFromSettings()`（`formBuilderUtils.ts`）— 項目のチェックをキャンペーン設定と連動
- `buildHtml()`（`formBuilderUtils.ts`）— 単体で動くHTMLの生成

### ⚠️ 直した本番の不具合

| # | 内容 |
|---|---|
| 1 | ⚠️ **公開フォームから任意の宛先へメールを送れた**（`mail_cc` のヘッダインジェクション） |
| 2 | ⚠️ **通知先をブラウザへ返していた**（`form_get` が `SELECT *`） |
| 3 | ⚠️ なごみの反響で `brand` が空（`nagomi` と `なごみ` の食い違い・実データ20件） |
| 4 | ⚠️ `sync` / `delete_flag` が数値列。42列の NOT NULL を全部埋めた |
| 5 | ⚠️ キャンペーンIDが違うとフォームが**真っ白**（原因も出ない） |
| 6 | ⚠️ 更新が0行でも「修正に成功しました」 |
| 7 | ⚠️ `form_update` に brand 条件が無く**別ブランドを巻き込んで更新** |
| 8 | ⚠️ アンケートURLが空でも案内文だけ届く |
| 9 | ⚠️ 会員登録メールの**署名が本文に入っていない**（文字列が孤立） |
| 10 | ⚠️ `inquiry_date` の形式が2種類 |
| 11 | ⚠️ khg の `mail_cc` が **`※ここは編集しない`**（14フォームで壊れたヘッダ） |
| 12 | ⚠️ 旧APIが**ダッシュボードの認証を通っていなかった** |

### ⚠️ 未デプロイ・要作業

- ⚠️ **`.env.prod` の `CORS_ORIGINS` に8オリジン追加**（⚠️ 忘れると毎回 ① へ落ちる）
- ⚠️ 8サイトへ `form-proxy.php` と公開フォームを配布
- ⚠️ **DBパスワードの変更**（平文でソースに入っている）
- ⚠️ `form_table.mail_cc` の khg 14件がプレースホルダのまま

---

## 3. 公開フォーム（`react/form_get`）

⚠️ **このプロジェクトは git 管理外**。⚠️ 変更の控えが無いので配布前にバックアップすること。

### 追加したディレクトリ・ファイル

`src/api/`

| ファイル | 内容 |
|---|---|
| `gateway.ts` | ⚠️ ② を叩き、失敗したら ① へ回す |
| `brands.ts` | ロゴ・ブランド解決・サンクスページ（⚠️ 2ファイルの重複を集約） |

### 変更したファイル

`src/components/Form.tsx` / `Confirm.tsx`

### ⚠️ 落とし穴

⚠️ `mail_to` / `mail_cc` を**空で固定してはいけない**。⚠️ ① へ退避したとき**社内通知が飛ばなくなる**（気づけない）。

---

## 4. 他社広告ライブラリ（MetaAds）

### 追加したファイル

| ディレクトリ | ファイル |
|---|---|
| `backend-express/src/features/` | `metaAds.ts` |
| `frontend/src/components/header/` | `metaAdsUtils.ts` / `MetaAdsSummary.tsx` |

### 追加した関数（`metaAdsUtils.ts`）

- `summarizeByAdvertiser()` — 広告主ごと（バナー数・見出し数・1見出しあたりの展開数）
- `summarizeByTitle()` — 見出しごと
- `summarizeByMonth()` — ⚠️ **収集開始月より前を除外**して返す
- `startMonth()` / `startDate()`

### ⚠️ データについて分かったこと

- ⚠️ **「掲載期間」は作れない**。`advertiser_period` は開始日のみ、`ad_hash` は UNIQUE
- ⚠️ 掲載開始日の **30%（1,132件）が空か壊れている**
- ⚠️ 収集開始は **2026-06-23**。それ以前の月（619件）はグラフに出さない
- ⚠️ SQL の既定照合は大文字小文字を同一視するため、⚠️ **画面の集計と1件ずれる**

---

## 5. 失注一覧の Express 化

| ディレクトリ | ファイル |
|---|---|
| `backend-express/src/features/` | `lostList.ts`（⚠️ 新規） |
| `frontend/src/components/` | `LostStatusList.tsx` |

⚠️ 移植元は `master_data` を**全件（24,697行）**返していた。⚠️ SQL 側で失注に絞り、⚠️ **14.3MB → 1.5MB（89%減）**。

---

## 6. 失注先不明ボタン

| ディレクトリ | ファイル |
|---|---|
| `frontend/src/components/information/` | `TableStatus.tsx` |
| `frontend/src/utils/` | `informationUtils.ts`（⚠️ `UNKNOWN_COMPETITOR` を追加） |

⚠️ 同じ判定が **3か所**にある（`DatabaseOrder.tsx` / `LostStatusList.tsx` / `menu.ts`）。⚠️ **片方だけ直さないこと。**

---

## ⚠️ 未デプロイ・未確認の一覧

| # | 内容 | 優先 |
|---|---|---|
| 1 | ⚠️ **`form-proxy.php` を8サイトへ**（メールの穴が開いたまま） | ⚠️ **最優先** |
| 2 | ⚠️ **DBパスワードの変更** | 高 |
| 3 | ⚠️ sync の `git push heroku main`（経緯度の修正・コミット済み） | 高 |
| 4 | ⚠️ `customer_address.php` を ① へ | 高 |
| 5 | ⚠️ `v2.2.131` のデプロイ | 中 |
| 6 | ⚠️ `v2.2.132` / `v2.2.133` の push とデプロイ | 中 |
| 7 | ⚠️ **画面を開いての動作確認**（キャンペーン・MetaAds・失注一覧とも未実施） | 中 |
| 8 | ⚠️ `master_data_resale` の id が NULL の行 | 低 |

---

## ⚠️ 費用について

| | 内容 |
|---|---|
| Geocoding | ⚠️ 先月 ¥150,000 超過。⚠️ 修正後は**無料枠内の見込み** |
| Places API | ⚠️ 競合収集で **$30〜$45**（`places.websiteUri` が Enterprise だったため） |

⚠️ 競合他社の Place ID 収集は**中止**（コスト過大）。⚠️ コードは `projects/sync` に未コミットで残っている。
