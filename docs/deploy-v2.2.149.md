# デプロイ手順 v2.2.149

⚠️ 中身: ⚠️ **Nexus へ顧客データを移行できる形に揃える。**

| # | 何が変わるか | どこ |
|---|---|---|
| 1 | ⚠️⚠️ **既存顧客のフリガナがカタカナになる** | ⚠️ ① DB |
| 2 | ⚠️⚠️ **保存時に「姓名間の半角スペース」「フリガナがカタカナ」を確認する**（注文のみ） | ⚠️ ① フロント |
| 3 | ⚠️ **印が出る**（⚠️ 一覧＝**丸囲みの `N`** ／ ⚠️ 顧客情報編集＝**`Nexus連携済み`**） | ⚠️ ① フロント |
| 4 | ⚠️ placeholder が **フリガナ①②** になる | ⚠️ ① フロント |
| 5 | ⚠️ **リストからの登録時にフリガナをカタカナへ直す**（注文・建売・中古） | ⚠️ ① フロント |
| 6 | ⚠️⚠️ **顧客一覧の応答にフリガナが加わる**（注文のみ） | ⚠️ ① PHP ＋ ⚠️ **② VPS** |
| 7 | 更新履歴に1行増える | ① DB |

⚠️⚠️ **② VPS の再ビルドが必要**（⚠️ `queries.ts` を直したため）。
⚠️ ⚠️ **MCP は変更なし。** ⚠️ **テーブル定義も変えていない**（列の追加・削除は無い）。

---

## ⚠️⚠️ v2.2.148 が未デプロイなら先に通すこと

⚠️ `v2.2.149` ブランチは `v2.2.148` から切ってある。
⚠️ ⚠️ **[deploy-v2.2.148.md](deploy-v2.2.148.md) を先に通すこと。** ⚠️ **PR は1本で足りる。**

---

## ⚠️ 順序

```
1. ① SQL（フリガナの置換）   ← ⚠️ 先に流すこと
2. ② VPS（Express の再ビルド）
3. ① PHP（database_order.php）
4. ① SQL（update_log）
5. ① フロント
```

⚠️⚠️ **手順1を後回しにしないこと。** ⚠️ **先にフロントを上げると、ひらがなの顧客を開いた人が全員 alert で止まる。**

---

## 手順1　【① レンタルサーバーで実行】フリガナをカタカナへ（phpMyAdmin）

⚠️ ファイル: `backend/scripts/sql/2026-09-25_kana_to_katakana.sql`

⚠️⚠️ **上から1ブロックずつ実行すること。** ⚠️ **まとめて流さないこと。**

| 手順 | 内容 | 見るもの |
|---|---|---|
| 1-1 | これから変換される件数 | ⚠️ 0件なら手順2〜4は不要 |
| 1-2 | ⚠️ **半角カナの件数** | ⚠️⚠️ **このSQLでは直らない。** 件数を控えておく |
| 2 | ⚠️ **退避テーブルを作る** | ⚠️⚠️ **飛ばさないこと。戻せなくなる** |
| 3-1〜3-12 | ⚠️ **UPDATE 12本** | ⚠️⚠️ **1本も飛ばさないこと** |
| 4 | 検証 | ⚠️⚠️ **すべて 0 になること** |

⚠️ ローカルDBでは ⚠️ **8,745件＋40件が変換され、検証はすべて 0** になった。
⚠️ 何度実行しても結果は変わらない（カタカナはもう置換されない）。

---

## 手順2　【② VPS で実行】Express の再ビルド

⚠️ ⚠️ **`git pull` は使わないこと。** ⚠️ **必ず分岐エラーになる。**

```bash
cd ~/dashboard
git fetch --depth 1 origin production
git checkout FETCH_HEAD
```
```bash
dcp build express-api
```
```bash
dcp up -d express-api
```

---

## 手順3　【あなたのPC（PowerShell）】PHP → ① へアップロード

| ファイル | |
|---|---|
| `backend/src/handlers/databaseAction/database_order.php` | ⚠️ **1行追加**（フリガナを返す） |

⚠️ ⚠️ **他の PHP は変えていない。**

---

## 手順4　【① レンタルサーバーで実行】SQL（phpMyAdmin）

```sql
INSERT INTO update_log (version, date, note) VALUES
('2.2.149', '2026-09-25', '顧客情報の登録時に、顧客名の姓名間の半角スペースとフリガナのカタカナを確認するようにした。Nexusへ移行できる顧客にアイコンを表示する。');
```

⚠️ ファイル: `backend/scripts/sql/2026-09-25_update_log_2.2.149.sql`

---

## 手順5　【あなたのPC（PowerShell）】フロント → ① へアップロード

⚠️ ⚠️ **ビルド済み**（2026-09-25）。

| ファイル | |
|---|---|
| ⚠️ **`build/static/js/main.cff2c660.js`** | ⚠️ **`2.2.149` を含む** |
| `build/static/css/main.7c10f266.css` | ⚠️ **変わっていない**（一緒に上げてよい） |
| ⚠️ **`build/index.html`** | ⚠️⚠️ **必ず差し替える** |

⚠️⚠️ **古いビルドを使わないこと。**
⚠️ `main.51fd52d1.js` / `main.9a189d75.js` … ⚠️⚠️ **同じ v2.2.149 だがアイコンの形が古い**
⚠️ `main.3bfc5388.js` … ⚠️ **v2.2.148**
⚠️ `main.7f7ac5e8.js` … ⚠️ **v2.2.147**

⚠️⚠️ **コードを直したら必ずビルドし直し、ハッシュが変わったことを確かめること。**

---

## ⚠️ 手順6　動作確認

### 顧客情報編集（注文）

| # | やること | 期待 |
|---|---|---|
| 1 | 顧客名が `国分 太郎`・フリガナが `コクブ タロウ` の顧客を開く | ⚠️⚠️ **見出しの左に `Nexus連携済み`**（ネイビー・白文字） |
| 2 | フリガナ欄の placeholder | ⚠️ **フリガナ①／フリガナ②** |
| 3 | 顧客名を `国分太郎` にして保存 | ⚠️⚠️ **`姓名間に半角スペースを入力すること。例）×国分太郎 〇国分 太郎`** で止まる |
| 4 | ⚠️ 全角スペースで `国分　太郎` にして保存 | ⚠️⚠️ **同じく止まる**（半角のみ） |
| 5 | フリガナを `こくぶ タロウ` にして保存 | ⚠️⚠️ **`ふりがなはカタカナで入力すること。`** で止まる |
| 6 | ⚠️ フリガナを空にして保存 | ⚠️⚠️ **止まらない**（未入力は咎めない） |
| 7 | 3〜5 で止まったあと | ⚠️⚠️ **保存ボタンがもう一度押せる** |
| 8 | 正しい形にして保存 | ⚠️ **保存できる**（⚠️ 再度開くと `Nexus` が出る） |
| 9 | ⚠️ **建売・中古の顧客情報編集** | ⚠️⚠️ **何も変わっていない**（alert もアイコンも出ない） |

### 顧客データベース（注文）

| # | やること | 期待 |
|---|---|---|
| 10 | 顧客名の列 | ⚠️⚠️ **ギフトのドットの隣に、ネイビーの丸囲み `N`**（⚠️ **改行しない**） |
| 11 | ⚠️ 表の上の凡例 | ⚠️⚠️ **「ギフト進呈不可」の右隣に `N 国分Nexus連携済み`** |
| 12 | ⚠️ 全件のうち | ⚠️ **8割前後に付く**（ローカルでは 19,486/24,609） |
| 13 | ⚠️ **建売・中古のデータベース** | ⚠️⚠️ **何も変わっていない**（⚠️ 凡例も2つのまま） |

### リストからの登録

| # | やること | 期待 |
|---|---|---|
| 14 | ⚠️ **ひらがなのフリガナ**の反響を同期 | ⚠️⚠️ **カタカナで登録される** |
| 15 | 建売・中古のリストでも同期 | ⚠️ **同じくカタカナ** |

| # | | |
|---|---|---|
| 16 | バージョン表示 | ⚠️ **2.2.149** |

---

## ⚠️ 戻し方

| 何 | どう戻すか |
|---|---|
| フロント | 1つ前の `main.*.js` と `index.html` |
| PHP | ⚠️ 追加した1行を消す |
| ⚠️ **顧客一覧API（②）** | ⚠️ ② を1つ前のコミットで再ビルドする |
| ⚠️⚠️ **フリガナの置換** | ⚠️ SQLファイル末尾の「戻し方」（⚠️ **退避テーブルと JOIN する UPDATE**）。⚠️ **コメントを外して実行する** |
| `update_log` の行 | ⚠️ 残しておいてよい |

⚠️⚠️ **退避テーブルを作ったあとに別の更新が入っていると、その更新も巻き戻る。** ⚠️ **戻す前にオーナーへ確認すること。**

---

## ⚠️ 申し送り

| # | 内容 |
|---|---|
| 1 | ⚠️⚠️ **半角カナは直らない。** ⚠️ 手順1-2 の件数を控え、⚠️ **手で直すこと**（ローカルでは35件） |
| 2 | ⚠️⚠️ **顧客名の注意書きはオーナー対応。** ⚠️ 今回は入れていない |
| 3 | ⚠️ 退避テーブル `master_data_kana_backup_20260925` は ⚠️ **数日おいてから消すこと** |
| 4 | ⚠️⚠️ **判定を変えるときは `frontend/src/utils/nexusUtils.ts` だけを直す。** ⚠️ 画面3箇所すべてがここを通っている |
| 5 | ⚠️⚠️ **`database_order.php` と `queries.ts` は必ず揃えること。** ⚠️ 片方だけだと ⚠️ **アイコンが出たり消えたりする** |
| 6 | ⚠️ ブラウザでの表示は ⚠️⚠️ **未確認**（ヘッドレスブラウザが無い） |
| 7 | ⚠️⚠️ **アイコンを四角い「Nexus」タグに戻さないこと。** ⚠️ **リンクに見える**ため丸囲みの `N` にしてある |
| 8 | ⚠️⚠️ **Nexus へ直接リンクする案は 2026-09-25 に見送った。** ⚠️ Nexus の ID は**乱数の UUID** で ULID から導けない |
| 7 | ⚠️⚠️ **MCP の説明文「既定の reaction」が古いまま。** ⚠️ **次に MCP を配布する版で直すこと**（v2.2.148 からの持ち越し） |
