# 指示（2026-09-18）　失注の必須項目を2つに絞る／建売に店舗別広告費を戻す

⚠️ 依頼①: 「`budget` の `medium` も表記のブレをマッピングしてほしい。⚠️ **もう処理済みなら対応不要**。
また `category === spec` のときに**店舗別広告費も表に出す**。前は出ていたような気がする」

⚠️ 依頼②（`ReadMeClaude.md`）: 「`InformationEdit.tsx` 及び `TableStatus.tsx` の改修。
⚠️ **失注理由: 競合負けのときの必須項目を `customized_input_01JRF9CZSW65A151WR30NA4PB3` と
`customized_input_01JSE7H4MQES619NBWX6PQDFRH` のみにする。**
それにともなう `DatabaseOrder.tsx` の `loseLength` や `Menu.tsx` への影響も配慮して UI 及び集計関数を修正する」

---

## ⚠️ 依頼① への回答

### ⚠️ `budget.medium` のマッピング → ⚠️ **対応不要（処理済み）**

⚠️ `CustomerKaeru.tsx`（販促媒体別広告費）では ⚠️ **すでに `normalizeMedium()` を通している**
（[task-2026-09-18-01](task-2026-09-18-01-kaeru-medium-mapping.md)）。

| 場所 | 状態 |
|---|---|
| 行の名前 | ⚠️ 通している |
| 顧客の突合 | ⚠️ 通している |
| ⚠️ **販促費の突合** | ⚠️ **通している** |

### 建売に「店舗別広告費」を戻した

⚠️ `Menu.tsx` の1行を戻しただけ。⚠️ **画面も ② の `shop:spec` も消していなかった。**

```tsx
        { id: 'shop', path: '/shop', icon: 'fa-chart-pie', label: '店舗別広告費', show: !isSp && (category === 'order' || category === 'spec'), exact: true },
```

⚠️ 2026-09-16 に「要件を整理するので一旦消す」と言われて外した経緯がある。

### ⚠️⚠️ 戻したことで見えてくる不整合（⚠️ **未対応。判断待ち**）

⚠️ `shop/ShopKaeru.tsx`（店舗別広告費）は ⚠️ **今回のマッピングを通っていない。**

| 箇所 | 状態 |
|---|---|
| 「全販促媒体」の絞り込み（顧客側） | ⚠️ `item.medium === selectedMedium` の**素の比較** |
| ⚠️ 販促費側 | ⚠️ **そもそも媒体で絞っていない** |

⚠️ 建売で媒体に「Web検索」を選ぶと ⚠️ **212件しか拾えない**（⚠️ `ネット` の **4,892件**が漏れる）。
⚠️ さらに販促費は絞られないため ⚠️ **単価が実態と大きくずれる。**

⚠️ ⚠️ **元からある不整合**で、直すと数字が変わるため ⚠️ **勝手に触っていない。**
⚠️ 契約率の分母も食い違ったまま（店舗別＝接触数／販促媒体別＝申込）。

---

## ⚠️ 依頼② の実データ（ローカルDB・2026-06-01 以降の失注 328件）

| | 件数 |
|---|---|
| 競合負け | 119件 |
| ⚠️ **改修前の「要回答」** | ⚠️ **89件** |
| ⚠️ **改修後の「要回答」** | ⚠️ **4件** |
| ⚠️ 保存が止まる件数 | ⚠️ **3件** |

⚠️ 内訳（競合負け 119件のうち空のもの）

| 項目 | 空 | 扱い |
|---|---|---|
| 失注理由 | 0 | 要回答（従来どおり） |
| 失注先 | 4 | ⚠️ **要回答に残す**（⚠️ **保存は止めない**） |
| ⚠️ 他決理由 | 1 | ⚠️ **必須（新規）** |
| ⚠️ 敗因 | 3 | ⚠️ **必須（維持）** |
| ⚠️ 価格差・今後の対策 | ⚠️ **119（全件）** | ⚠️ **必須をやめ、要回答からも外す** |

⚠️⚠️ **価格差と今後の対策は新しい列なので既存が全件空**である。
⚠️ 残すと ⚠️ **永久に要回答が減らない。**

---

## ⚠️ 着手前に確認したこと（利用者が決定）

| # | 論点 | ⚠️ 決定 |
|---|---|---|
| 1 | 「失注先」は要回答に残すか | ⚠️ **残す**（⚠️ 「失注先不明」ボタンはそのためにある） |

---

## 変更したファイル

| ディレクトリ | ファイル | 内容 |
|---|---|---|
| `frontend/src/utils/` | ⚠️ `informationUtils.ts` | ⚠️ **`LOST_FIELDS` を3つに** |
| `frontend/src/components/information/` | `TableStatus.tsx` | ⚠️ 必須の印を付け替え |
| `frontend/src/components/` | `Menu.tsx` | ⚠️ 建売に店舗別広告費 |
| `backend-express/src/features/` | `menu.ts` | ⚠️ `LOST_REQUIRED_COLUMNS` から2つ外す |
| `backend/src/handlers/` | ⚠️ `menu.php` | ⚠️ **② と同じ（フォールバック分）** |

⚠️ ⚠️ **`DatabaseOrder.tsx` / `LostStatusList.tsx` は変更していない。**
⚠️ すべて `missingLostFields()` を呼んでいるため ⚠️ **自動で追従する**（2026-09-17 の集約が効いた）。

---

## ⚠️ 変更の中身

### `informationUtils.ts`（⚠️ 差し替えた全文）

```ts
/**
 * ⚠️⚠️ **一覧の「未入力箇所」に出す項目。並び順もこのとおりに出る。**
 *   ⚠️ **任意の項目（他社営業・他社のキャンペーン）は入れないこと。**
 *     ⚠️ 入れると**埋めようのない項目で要回答が減らなくなる。**
 *
 * ⚠️⚠️ **2026-09-18 に価格差・今後の対策をこの一覧から外した**（指示）。
 *   ⚠️ 必須は ⚠️ **他決理由と敗因の2つだけ**になった。
 *   ⚠️ ⚠️ **一覧（要回答）からも外している。**
 *     ⚠️ **残すと要回答が減らない。** 新しい列なので**既存が全件空**で、
 *       ⚠️ ローカルの実測では **328件中89件**が永久に要回答のままだった。
 *     ⚠️ ⚠️ **必須と要回答は必ず同じ顔ぶれにすること。**
 *       ⚠️ 片方だけ減らすと、⚠️ **一覧に出るのに直しようがない**
 *         （または**直したのに件数が減らない**）状態になる。
 *   ⚠️ 入力欄そのものは残してある（⚠️ **任意項目**として）。
 *
 * ⚠️ 失注先は ⚠️ **要回答に残す**（2026-09-18 に利用者が決定）。
 *   ⚠️ 2026-09-17 より前から要回答の対象で、⚠️ **「失注先不明」ボタンはそのためにある。**
 *   ⚠️ ただし ⚠️ **保存は止めない**（`blocksSave: false`）。従来どおり。
 *
 * ⚠️⚠️ **② の menu.ts と ① の menu.php の `LOST_REQUIRED_COLUMNS` と同じ顔ぶれにすること。**
 *   ⚠️ 食い違うと**メニューのバッジと失注一覧の件数が合わない。**
 */
export const LOST_FIELDS: LostField[] = [
    { key: COMPETITOR_KEY, blocksSave: false },
    { key: LOST_DETAIL_KEY, blocksSave: true },
    { key: LOSE_REASON_KEY, blocksSave: true },
];
```

### `TableStatus.tsx`（他決理由に必須の印）

```tsx
                            {/**
                              * ⚠️⚠️ **この欄が「他決理由」で、2026-09-18 から必須になった**（指示）。
                              *   ⚠️ 必須の印は注文事業だけに出す（⚠️ **建売・中古では保存を止めない**）。
                              *   ⚠️ 判定は informationUtils の `LOST_FIELDS.blocksSave`。**揃えること。**
                              */}
                            <div className="fw-bold mb-2 text-secondary mt-3" style={{ fontSize: '12px' }}>
                                詳細な他決・失注理由（複数選択可）
                                {isOrder && <span style={requiredStyle}>必須</span>}
                                {isOrder && isBlank(information[LOST_DETAIL_KEY]) &&
                                    <i className="fa-solid fa-triangle-exclamation text-danger ms-2"></i>}
                            </div>
```

### `TableStatus.tsx`（価格差・今後の対策を任意に）

```tsx
                                    {/**
                                      * ⚠️⚠️ **2026-09-18 に価格差・今後の対策の必須をやめた**（指示）。
                                      *   ⚠️ 必須は ⚠️ **他決理由と敗因の2つだけ**である。
                                      *   ⚠️ ⚠️ **判定は informationUtils の `LOST_FIELDS.blocksSave`。**
                                      *     ⚠️ ここの `required` は**見た目の印だけ**なので、
                                      *       ⚠️ **向こうと必ず揃えること。** 食い違うと
                                      *       「必須と出ているのに保存できる」「任意なのに止まる」になる。
                                      */}
                                    {/* ⚠️ 単位は契約済み側と必ず同じにする（同じ列に入るため） */}
                                    {freeField(PRICE_GAP_KEY, false, 'number', '他社との差額', PRICE_GAP_UNIT)}
                                    {freeField(COUNTERMEASURE_KEY, false, 'textarea', '次に同じ競合と当たったときの対策')}
```

### ② `menu.ts`

```ts
const LOST_REQUIRED_COLUMNS = [
    'competitor_name',
    'customized_input_01JRF9CZSW65A151WR30NA4PB3',
    'customized_input_01JSE7H4MQES619NBWX6PQDFRH',
    /**
     * ⚠️⚠️ **2026-09-17 に価格差・今後の対策を足したが、2026-09-18 に外した**（指示）。
     *   ⚠️ 新しい列なので**既存が全件空**で、⚠️ **要回答がほぼ全件になっていた。**
     *   ⚠️ ローカルの実測で **89件 → 4件**。
     *   ⚠️ ⚠️ **フロントの `LOST_FIELDS` からも外れている。** 片方だけ戻さないこと。
     *
     * 'competitor_price_gap',
     * 'competitor_countermeasure',
     */
];
```

### ① `menu.php`（⚠️ **フォールバック分**）

```php
$lost_required_columns = [
    'competitor_name',
    'customized_input_01JRF9CZSW65A151WR30NA4PB3',
    'customized_input_01JSE7H4MQES619NBWX6PQDFRH',
    // ⚠️⚠️ **2026-09-17 に価格差・今後の対策を足したが、2026-09-18 に外した**（指示）。
    //   ⚠️ 新しい列なので既存が全件空で、⚠️ **要回答がほぼ全件になっていた。**
    //   ⚠️ ⚠️ **② の menu.ts とフロントの LOST_FIELDS からも外れている。**
    //     ⚠️ 片方だけ戻さないこと。
    // 'competitor_price_gap',
    // 'competitor_countermeasure',
];
```

---

## 検証

### ⚠️⚠️ フロントと ② で件数が一致すること

⚠️ `informationUtils.ts` を単体で JS に変換し、⚠️ **② の SQL と同じ328件**を通した。

| | 件数 |
|---|---|
| 対象 | 328 |
| ⚠️ **フロントの要回答** | ⚠️ **4** |
| ⚠️ **② の `menu` が返す `lost`** | ⚠️ **4** |
| 保存が止まる件数 | 3 |

⚠️ 内訳: 失注先 4 ／ 敗因 3 ／ 他決理由 1

### ⚠️⚠️ 途中で食い違いを踏んだ

⚠️ はじめ ⚠️ **`blocksSave` だけを false にして一覧には残した**ところ、
⚠️ ⚠️ **フロント 89件 / ② 4件**と食い違った。
⚠️ ⚠️ **必須と要回答は同じ顔ぶれでなければならない。** ⚠️ 一覧からも外して解決した。

### ⚠️ ローカルDBが作り直されていた

⚠️ 作業中に ⚠️ **ローカルの MariaDB が作り直され、v2.2.136 の5列が消えていた。**
⚠️ `2026-09-17_master_data_win_lose.sql` を流し直して復旧した。
⚠️ そのため ⚠️ **2026-09-17 の記録にある「39件 → 108件」とは母数が違う**（今回は328件が対象）。

### ビルド・型

| 確認 | 結果 |
|---|---|
| `npx tsc --noEmit`（② Express） | ⚠️ **エラー0** |
| `npm run build`（フロント） | ⚠️ **成功** |
| ⚠️ 追加した警告 | ⚠️ **無し**（`TableStatus.tsx` の `authority` 未使用は**改修前からある**） |
| `php -l`（`menu.php`） | ⚠️ **構文エラーなし** |

### ⚠️ 未実施

| # | 確認 | 期待 |
|---|---|---|
| 1 | 失注 → 競合負けで空のまま保存 | ⚠️ **「他決理由」→「敗因」の順で止まる** |
| 2 | ⚠️ 価格差・今後の対策を空のまま保存 | ⚠️ **止まらない**（⚠️ 見出しは「任意」） |
| 3 | 「詳細な他決・失注理由」 | ⚠️ **必須の赤い印が出る**（⚠️ 注文事業のみ） |
| 4 | 反響一覧の「要回答」 | ⚠️ **大きく減る** |
| 5 | メニューの失注バッジ | ⚠️ **反響一覧と同じ件数** |
| 6 | 失注リストの「未入力箇所」 | ⚠️ **価格差・今後の対策が出ない** |
| 7 | ⚠️ 建売でメニュー | ⚠️ **「店舗別広告費」が出る** |
| 8 | ⚠️ 建売・中古の顧客詳細 | ⚠️ **保存できる**（必須の判定は注文事業のみ） |

---

## ⚠️ 判断を待っていること

| # | 内容 |
|---|---|
| 1 | ⚠️ `ShopKaeru.tsx` に媒体マッピングを通すか（⚠️ **数字が変わる**） |
| 2 | ⚠️ `ShopKaeru.tsx` の販促費を**媒体で絞る**か（⚠️ 現在は絞っていない） |
| 3 | ⚠️ `ShopKaeru.tsx` の契約率の分母を「申込」に揃えるか |
