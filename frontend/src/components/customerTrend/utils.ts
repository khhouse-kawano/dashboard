/**
 * 販促媒体別動向（customerTrend）で共有する値。
 *
 * ⚠️ 画面ごとに書き分けないこと。Order と Kaeru で色が食い違うと、
 *   同じ媒体が別の色で出て読み違える。
 */

/**
 * 積み上げ棒グラフの系列色（販促媒体ごと）。
 *
 * ─────────────────────────────────────────────
 * ⚠️⚠️ **彩度を落とした配色にしている（2026-09-11 変更）。**
 *   以前は Bootstrap の原色（#0d6efd / #dc3545 など）をそのまま並べており、
 *   18系列を積み上げると彩度が高すぎて目が疲れる、という指摘があった。
 *
 *   採用したのは Tableau 10 とその拡張（Tableau 20）の系統。
 *   ⚠️ 見た目の好みで選んだのではなく、**隣り合う色の区別がつくよう
 *     色相を散らしてある**データ可視化用のパレットである。
 *   淡いパステルに寄せると、18系列では隣同士が見分けられなくなる。
 *
 * ⚠️ **順番を入れ替えないこと。** 添字が販促媒体の並び順に対応しており、
 *   入れ替えると利用者が覚えている「この色はSUUMO」が変わる。
 *   色を足すときは**末尾に足す**。
 *
 * ⚠️ 媒体数がこの配列より多いと `undefined` になり、recharts の既定色
 *   （黒に近い色）で描かれる。現在18色で、実データの媒体数を上回っている。
 * ─────────────────────────────────────────────
 */
export const chartColors = [
    "#4e79a7", // くすんだ青
    "#f28e2b", // くすんだ橙
    "#e15759", // くすんだ赤
    "#76b7b2", // くすんだ青緑
    "#59a14f", // くすんだ緑
    "#edc948", // くすんだ黄
    "#b07aa1", // くすんだ紫
    "#ff9da7", // くすんだ桃
    "#9c755f", // くすんだ茶
    "#bab0ac", // 灰
    "#86bcb6", // 淡い青緑
    "#a0cbe8", // 淡い青
    "#ffbe7d", // 淡い橙
    "#8cd17d", // 淡い緑
    "#f1ce63", // 淡い黄
    "#d4a6c8", // 淡い紫
    "#d7b5a6", // 淡い茶
    "#79706e", // 濃い灰
];
