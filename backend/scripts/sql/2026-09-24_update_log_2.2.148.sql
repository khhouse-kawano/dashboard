-- =====================================================================
-- update_log に v2.2.148 を追加する
--
-- ⚠️⚠️ **版を上げる指示を受けたら、`version.ts` と同時にこの行も足すこと**
--   （2026-09-21 の指示。⚠️ **デプロイ直前ではなく着手時に作る。**）
--
-- ⚠️ `no` は AUTO_INCREMENT なので指定しないこと。
-- ⚠️ 実行環境: ① レンタルサーバー（phpMyAdmin）
-- =====================================================================

INSERT INTO update_log (version, date, note) VALUES
('2.2.148', '2026-09-25', '契約ランキングの達成率を独立した列にし、達成率でも並べ替えられるようにした。分析APIの実績日起算と販促媒体を反響推移の画面に揃えた。');
